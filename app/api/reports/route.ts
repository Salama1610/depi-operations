import {actor,auditStmt} from '@/lib/server';import {report} from '@/lib/reports';import {toCSV,toXLSX} from '@/lib/spreadsheet';
export async function GET(req:Request){try{const u=await actor();const q=new URL(req.url).searchParams;const data=await report(u,q.get('from')||undefined,q.get('to')||undefined);const format=q.get('format');if(format){await auditStmt(u,'Report export','reports',{from:q.get('from'),to:q.get('to'),format}).run();return new Response(format==='xlsx'?toXLSX(data.groups).buffer as ArrayBuffer:toCSV(data.groups),{headers:{'Content-Type':format==='xlsx'?'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet':'text/csv','Content-Disposition':`attachment; filename="depi-group-report.${format==='xlsx'?'xlsx':'csv'}"`,'Cache-Control':'private,no-store'}})}return Response.json(data,{headers:{'Cache-Control':'private,no-store'}})}catch(e:any){return Response.json({error:e.message},{status:400})}}

import {env} from 'cloudflare:workers';import {actor,permit,all,stmt,auditStmt,db,uid,now,rateLimit} from '@/lib/server';import {ensure} from '@/lib/domain/rules';
export async function GET(){try{const u=await actor();await rateLimit('system:'+u.id,60,60);permit(u,['Operations Systems / Admin']);return Response.json({connections:{automation:!!(env.AUTOMATION_HMAC_SECRET&&env.AUTOMATION_ACTOR_EMAIL),vault:!!(env.VAULT_URL&&env.VAULT_TOKEN),backup_encryption:!!env.BACKUP_ENCRYPTION_KEY},runs:await all('SELECT id,kind,status,result,created_at,updated_at FROM automation_runs ORDER BY created_at DESC LIMIT 50')},{headers:{'Cache-Control':'no-store'}})}catch(e:any){return Response.json({error:e.message},{status:403})}}
export async function POST(req:Request){try{const u=await actor();await rateLimit('system-write:'+u.id,20,60);permit(u,['Operations Systems / Admin']);ensure(!req.headers.get('origin')||req.headers.get('origin')===new URL(req.url).origin,'Cross-site action rejected.');const x=await req.json();ensure(x.action==='retry_run'&&x.reason?.trim(),'Choose a failed run and record the retry reason.');const run:any=await stmt('SELECT * FROM automation_runs WHERE id=?',x.id).first();ensure(run&&(run.status==='Failed'||run.status==='Running'&&Date.now()-Date.parse(run.updated_at)>15*60000),'Only failed or stale runs can be released for retry.');await db().batch([stmt("UPDATE automation_runs SET status='Retryable',updated_at=? WHERE id=? AND status=?",now(),x.id,run.status),auditStmt(u,'Automation retry authorized',x.id,{status:'Retryable'},run,uid('REQ'),x.reason)]);return Response.json({ok:true})}catch(e:any){return Response.json({error:e.message},{status:400})}}
